[project]
name = "plurality-collapse"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "praw>=7.7",
    "python-dotenv>=1.0",
    "datasets>=2.14",
    "openai>=1.0",
    "torch>=2.0",
    "transformers>=4.35",
    "numpy>=1.24",
]
